@model WorldTrackerDomain.Queries.PlaceGetByIDQueryResult

@{
    ViewData["Title"] = "Delete Place";
}

<h1>Delete Place</h1>

<h3>Are you sure you want to delete this?</h3>
<div>
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.PlaceGetByIDView.Place.Name)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.PlaceGetByIDView.Place.Name)
        </dd>
    </dl>

    <form asp-action="DeleteConfirmed" asp-route-id="@Model.PlaceGetByIDView.Place.ID">
        <input type="submit" value="Delete" class="btn btn-danger" /> |
        <a asp-action="Index" asp-controller="DeviceUpdateManifest">Back to People</a>
        <div asp-validation-summary="ModelOnly" class="text-danger"></div>
    </form>
</div>
